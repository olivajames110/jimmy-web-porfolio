import React, { Fragment } from 'react';
import './menu.css';

const Menu = (props) => {
	return (
		<Fragment>
			<div className="root-menu-container">Menu</div>
		</Fragment>
	);
};

export default Menu;
